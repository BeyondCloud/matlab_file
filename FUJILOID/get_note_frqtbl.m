% return frq tbl with size of the duration of the note in ms
function y = get_note_frqtbl(score)
%     if length(times) ~= length(times) 
%         error('expect times len == CC len');
%     end
%     y = zeros(CCs(end).t{1}-CCs(1).t{1}+1,1);
%     for i = 1:length(CCs)-1
%         note_PIT = (PITs/8192)*PBS+note;
%         f = 2^((note_PIT-64)/12)*440;
%     end

end