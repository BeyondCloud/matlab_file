Xia_path = 'C:\Program Files (x86)\UTAU\App\UTAU\voice\Xia_Voice_Bank_TZH';
oto_mid = fullfile(Xia_path,'mid','oto.ini')
% f = fullfile(Xia_mid,'_hao_hao.wav');
f = fopen(oto_mid)
C_mid = textscan(f, '%s%f%f%f%f%f', 'delimiter',',')
ly = C_mid(1);
for i = 1: 25
    out = regexp(ly{1}(i),'cow(.*)milk','match')
    disp(out)
% [x fs] = audioread(f);
end
